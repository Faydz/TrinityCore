sLog->outError(LOG_FILTER_GENERAL, "fcnasdcdnas§");
                for (std::list<WorldObject*>::const_iterator itr = targets.begin(); itr != targets.end(); ++itr)
                {
                    sLog->outError(LOG_FILTER_GENERAL, "gtttwewwdsa");
                    if((*itr)->GetTypeId() == TYPEID_PLAYER)
                    {
                        Player* pl = (*itr)->ToPlayer();
                        if(!pl->IsFriendlyTo(GetCaster()))
                        {
                            Unit::AuraEffectList const& aurEff = pl->GetAuraEffectsByType(SPELL_AURA_MOD_STEALTH);
                            if(aurEff.size() != 0)
                                pl->RemoveAurasByType(SPELL_AURA_MOD_STEALTH);
                        }
                    }
                }